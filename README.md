Chat app
========

Install dependencies
--------------------

PC ```npm install```
Mac ```sudo npm install```

Start web server
----------------

```node web-server```

To run the app, go to http://localhost:8000